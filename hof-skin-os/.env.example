# Browser (Vite) — these are the ONLY secrets the client may ever see.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Cloudflare Worker only — set via `wrangler secret put`, never in a committed file.
ANTHROPIC_API_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
